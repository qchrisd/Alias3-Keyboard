# Alias3-Keyboard
Drivers for an e-paper display keyboard.

## Important point
The actual runnable project needs a `main.py` file in the root directory. Micropico extension has a setting to determine the sync folder. Make sure to set this when trying to move files from your computer to the esp board.